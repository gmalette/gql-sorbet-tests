source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }
gem("sorbet", :group => :development)
gem("sorbet-runtime")
gem("graphql")

# Specify your gem"s dependencies in blog-gql-sorbet.gemspec
gemspec
